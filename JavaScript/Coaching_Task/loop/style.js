/*
let n = +prompt("enter how to start countdown");

if (n < 0) {
  console.log(
    "Loop condition false initially, negative values are not allowed."
  );
} else {
  while (n >= 0) {
    console.log(n);
    n--;
  }
  console.log("  --- Countdown End ---");
}
*/
  /*
 let enterPassword = prompt("enter your pasword")
   let correctPassword = "secret123"
   let attempts=3;

  if(enterPassword==correctPassword){
    console.log(" Login successful!")

   }
   else{
     while(prompt("enter your pasword")!=correctPassword || attempts!=0){


    console.log(`Enter your password. ${attempts} attempts left`)
   attempts--;

   }
   if(attempts==0){
     console.log("No attempts left. Account locked.")

   }
   }
  */
